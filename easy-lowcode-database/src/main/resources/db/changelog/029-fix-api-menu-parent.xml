<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.25.xsd">

    <changeSet id="029-fix-api-menu-parent" author="system">
        <validCheckSum>ANY</validCheckSum>

        <!-- 确保 resource 父菜单存在 -->
        <sql>
            INSERT INTO sys_menu (id, parent_id, menu_name, menu_code, menu_type, path, component, perms, icon, sort, visible, deleted, create_time, update_time, create_by, update_by)
            SELECT
                COALESCE((SELECT MAX(id) FROM sys_menu), 0) + 1,
                0,
                '资源管理',
                'resource',
                1,
                '/resource',
                'Layout',
                '',
                'DataLine',
                2,
                1,
                0,
                NOW(),
                NOW(),
                1,
                1
            WHERE NOT EXISTS (SELECT 1 FROM sys_menu WHERE menu_code = 'resource');
        </sql>

        <!-- 把 API管理菜单移到 resource 父菜单下（按 menu_name 匹配） -->
        <sql>
            UPDATE sys_menu
            SET parent_id = (SELECT id FROM (SELECT id FROM sys_menu WHERE menu_code = 'resource') AS t),
                sort = 3,
                icon = 'Link'
            WHERE menu_name = 'API管理' AND parent_id = 0;
        </sql>

        <!-- 添加数据源管理作为 resource 的子菜单 -->
        <sql>
            INSERT INTO sys_menu (id, parent_id, menu_name, menu_code, menu_type, path, component, perms, icon, sort, visible, deleted, create_time, update_time, create_by, update_by)
            SELECT
                COALESCE((SELECT MAX(id) FROM sys_menu), 0) + 1,
                (SELECT id FROM sys_menu WHERE menu_code = 'resource'),
                '数据源管理',
                'datasource_management',
                2,
                '/resource/datasource',
                'resource/DataSourceManagement',
                'resource:datasource:list',
                'Connection',
                1,
                1,
                0,
                NOW(),
                NOW(),
                1,
                1
            WHERE NOT EXISTS (SELECT 1 FROM sys_menu WHERE menu_code = 'datasource_management');
        </sql>

        <!-- 添加表资源注册作为 resource 的子菜单 -->
        <sql>
            INSERT INTO sys_menu (id, parent_id, menu_name, menu_code, menu_type, path, component, perms, icon, sort, visible, deleted, create_time, update_time, create_by, update_by)
            SELECT
                COALESCE((SELECT MAX(id) FROM sys_menu), 0) + 1,
                (SELECT id FROM sys_menu WHERE menu_code = 'resource'),
                '表资源注册',
                'table_resource_management',
                2,
                '/resource/table',
                'resource/TableResourceManagement',
                'resource:table:list',
                'DataBoard',
                2,
                1,
                0,
                NOW(),
                NOW(),
                1,
                1
            WHERE NOT EXISTS (SELECT 1 FROM sys_menu WHERE menu_code = 'table_resource_management');
        </sql>
    </changeSet>

</databaseChangeLog>